import { PriceVisiblePipe } from './price-visible.pipe';

describe('PriceVisiblePipe', () => {
  it('create an instance', () => {
    const pipe = new PriceVisiblePipe();
    expect(pipe).toBeTruthy();
  });
});
